import { Controller } from '@nestjs/common';

/**
 * REST Controller for User management.
 * Currently serves as a structural foundation for future expansions (e.g. user profile updates).
 */
@Controller('users')
export class UsersController {}
